/*******************************************************************************
 * $Workfile: $
 * $Logfile: $
 * $Revision: $
 * $Author: joneric Wennerstrom $
 *
 * © 2008 Beaglebuddy Software.   All rights reserved.
 *******************************************************************************/
package com.beaglebuddy.tab.conversion;

import com.beaglebuddy.tab.model.song.Song;
import com.beaglebuddy.tab.io.FileReadException;




/**
 * This utility class is responsible for converting guitar pro 5 (.gp5) to the beaglebuddy 1.0 version format.
 */
public class Converter_GP5_to_BBT_1_0
{
   /**
    * converts a guitar pro 5 file (.gp5) to a beaglebuddy 1.0 song.
    * <br/><br/>
    * @param filename   the name of the guitar pro 5 file file that is to be converted.
    * <br/><br/>
    * @return the converted guitar pro 5 file as a beaglebuddy 1.0 song.
    * <br/><br/>
    * @throws FileReadException  if the guitar pro 5 file can not be found, is not a valid guitar pro 5 file, or can not be read in and parsed.
    */
   public static Song convert(String filename) throws FileReadException
   {
/*    GP5File gp5File = loadGP5File(filename);
      Header  header  = new Header(convertInformation(ptbFile.getHeader()), convertChordDiagrams(ptbFile.getGuitarScore().getChordDiagrams(), ptbFile.getBassScore().getChordDiagrams()));
      Score   score   = convertScores(ptbFile.getGuitarScore(), ptbFile.getBassScore()); */
      Song    song    = null;

/*    if (score == null)
      {
         song = new Song();
         song.setHeader(header);
      }
      else
      {
         song = new Song(header, score);
      }
 */
      return song;
   }


   /**
    * loads a guitar pro 5 file.
    * <br/><br/>
    * @param gp5Filename name of the guitar pro 5 file to load.
    * <br/><br/>
    * @return the parsed guitar pro 5 file.
    * <br/><br/>
    * @throws FileReadException  if the guitar pro 5 file can not be found, is not a valid gp5 file, or can not be read in.
    */
/* private static GP5File loadGP5File(String ptbFilename) throws FileReadException
   {
      GP5File gp5File = null;
      try
      {
         gp5File  = new GP5File(gp5Filename);
      }
      catch (GP5FileException ex)
      {
         throw new FileReadException(ex.getMessage());
      }
      return gp5File;
   }
 */
}
